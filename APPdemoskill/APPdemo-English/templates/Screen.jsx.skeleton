// 新页面骨架：复制本文件 → 改名 Xxx.jsx 放 src/screens/ →
// 在 App.jsx 里 import 并加 <Route path="/xxx" element={<Xxx />} />。
import StatusBar from '../components/StatusBar'
// import xxxBg from '../assets/xxx-bg.png'   // 素材用 import 引入（Vite 会哈希进包）

export default function Xxx(/* { 需要的全局状态 } */) {
  return (
    <div className="screen xxx">
      {/* 图片/深色背景 → <StatusBar light /> 白字；浅色背景 → 不加 light */}
      <StatusBar light />

      {/* 背景图 / 主体图：按参考图用百分比 + z-index 定位 */}
      {/* <img className="xxx__bg" src={xxxBg} alt="" draggable={false} /> */}

      {/* 内容元素（头像、文字、气泡…） */}
    </div>
  )
}

/*
 * 配套 CSS 写进 src/screens.css，遵循：
 *   - .screen 基类已给 position:relative + 宽高100% + overflow:hidden + flex column
 *   - 元素定位用 position:absolute + 百分比（相对手机 390×844，基准尺寸见 00 §2）
 *   - z-index 层级见 references/00_project_conventions.md §10 的总表，就近取，别乱插
 *   - 颜色/字号走 :root 变量（见 references/00_project_conventions.md §6），别散落 hex
 */
