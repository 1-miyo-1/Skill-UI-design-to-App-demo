/* 各页面专属样式都写在这里（或按页拆成多个 css 再 import）。
   遵循：
   - 颜色/字号走 :root 变量（见 00 §6），别散落 hex
   - 定位用 position:absolute + 百分比（相对手机 390×844，基准尺寸见 00 §2）
   - z-index 见 00 §10 总表，就近取，别乱插
   - 元素类名带页面前缀（.home__title、.chat__bubble…），避免跨页撞名
 */
